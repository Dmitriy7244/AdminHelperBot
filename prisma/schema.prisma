generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["deno"]
  output          = "../generated/client"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
  directUrl = "postgresql://postgres:3*BMbSv@.AF69Dh@db.tiiybsbhmicwntwstsii.supabase.co:5432/postgres"
}

model Post {
  id         Int   @id @default(autoincrement())
  deleteTime Int
  messageIds Int[]
}
